# PrimeTrade Backend

This directory contains the FastAPI implementation of the PrimeTrade platform.

Run `poetry install` to install dependencies, then start the dev server with:

```bash
poetry run primetrade-api
```
